# QR on Product Slip

Add support of a QR code in the top left or right corder of a production slip. It should be added as a placeholder in production slip template like {{qr_code}}.

The qr code should contain a link to factory's manufacturing journal with id prefilled in the url as a parameter.