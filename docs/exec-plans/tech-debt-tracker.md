# Tech Debt Tracker

## due date fix 
Source of truth: settings control only, defaults to UTC
Scope
production slip as of now
User override
if user has a timezone on the frontend use his local timezone
Format
Use IANA timezone IDs (Europe/Riga, America/New_York) only?
UI location
Where should the setting control live (existing Settings page section/path) yes
Migration/default
What should the initial default be for existing installs?
UTC timezone
Due date behavior
Local date and time printed nothing else